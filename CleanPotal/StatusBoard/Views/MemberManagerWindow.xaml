<Window x:Class="CleanPotal.StatusBoard.Views.MemberManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="인원 관리" Width="400" Height="520"
        WindowStartupLocation="CenterOwner" Background="#F8FAFC"
        ResizeMode="NoResize" ShowInTaskbar="False"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
    <Window.Resources>
        <Style x:Key="Btn" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="Background" Value="#F1F5F9"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="bd" Property="Opacity" Value="0.5"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource Btn}">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
        </Style>
        <Style x:Key="DangerBtn" TargetType="Button" BasedOn="{StaticResource Btn}">
            <Setter Property="Background" Value="#FEE2E2"/>
            <Setter Property="Foreground" Value="#DC2626"/>
            <Setter Property="BorderBrush" Value="#FECACA"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="인원 관리" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
            <TextBlock Text="담당자를 추가·삭제하고 순서를 정합니다. 저장하면 일정표에 반영됩니다."
                       FontSize="12" Foreground="#64748B" Margin="0,3,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- 추가 입력 -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtNewName" Grid.Column="0" Height="36" FontSize="14" VerticalContentAlignment="Center"
                     Padding="10,0" BorderBrush="#CBD5E1" BorderThickness="1" KeyDown="TxtNewName_KeyDown">
                <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></TextBox.Resources>
            </TextBox>
            <Button Grid.Column="1" Content="+ 추가" Style="{StaticResource PrimaryBtn}" Padding="16,0" Height="36"
                    Margin="8,0,0,0" Click="BtnAdd_Click"/>
        </Grid>

        <!-- 목록 + 순서 버튼 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
                <ListBox x:Name="LstMembers" BorderThickness="0" Background="Transparent" Padding="4"
                         FontSize="14" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- 이름 인라인 수정: 셀 클릭 후 바로 편집 -->
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                     BorderThickness="0" Background="Transparent" FontSize="14"
                                     Padding="4,3" GotFocus="MemberBox_GotFocus"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="8,0,0,0">
                <Button Content="▲ 위로" Style="{StaticResource Btn}" Padding="10,7" Margin="0,0,0,6" Click="BtnUp_Click"/>
                <Button Content="▼ 아래로" Style="{StaticResource Btn}" Padding="10,7" Margin="0,0,0,6" Click="BtnDown_Click"/>
                <Button Content="삭제" Style="{StaticResource DangerBtn}" Padding="10,7" Click="BtnDelete_Click"/>
            </StackPanel>
        </Grid>

        <!-- 저장/취소 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="취소" Style="{StaticResource Btn}" Padding="20,8" Margin="0,0,8,0" Click="BtnCancel_Click"/>
            <Button Content="저장" Style="{StaticResource PrimaryBtn}" Padding="24,8" Click="BtnSaveMembers_Click"/>
        </StackPanel>
    </Grid>
</Window>
