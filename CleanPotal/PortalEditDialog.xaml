<Window x:Class="CleanPotal.PortalEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="포털 버튼 설정" Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ResizeMode="NoResize" Background="#F5F7FB">

    <Grid Margin="20,20,20,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="버튼 연결 설정" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
            <TextBlock Text="버튼에 표시될 이름과 실행할 파일/폴더 경로를 입력하세요." FontSize="12" Foreground="#6B7280" Margin="0,4,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="버튼 이름" FontSize="13" FontWeight="SemiBold" Foreground="#475467" Margin="0,0,0,6"/>
            <TextBox x:Name="TitleBox" Height="36" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#D6DCE8" BorderThickness="1"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="실행 경로 (파일, 폴더 또는 웹주소)" FontSize="13" FontWeight="SemiBold" Foreground="#475467" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox" Grid.Column="0" Height="36" Padding="8,0" VerticalContentAlignment="Center" BorderBrush="#D6DCE8" BorderThickness="1" Margin="0,0,8,0"/>
                <Button Grid.Column="1" Content="찾아보기" Width="70" Height="36" Background="#F3F6FB" BorderBrush="#D6DCE8" BorderThickness="1" Foreground="#344054" Click="BtnBrowse_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="취소" Width="70" Height="36" Background="#FFFFFF" BorderBrush="#D6DCE8" BorderThickness="1" Margin="0,0,8,0" Click="BtnCancel_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="저장" Width="70" Height="36" Background="#2563EB" Foreground="White" BorderThickness="0" Click="BtnSave_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>