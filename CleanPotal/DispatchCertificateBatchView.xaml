<UserControl x:Class="CleanPotal.DispatchCertificateBatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F1F5F9">
    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="반출등록 성적서 일괄 생성" FontSize="26" FontWeight="ExtraBold" Foreground="#0F172A"/>
            <TextBlock Text="기준정보/반출등록/생성이력 시트를 기준으로 템플릿 파일을 수량만큼 자동 생성합니다." FontSize="14" Foreground="#64748B" Margin="0,8,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="엑셀 파일" VerticalAlignment="Center" FontWeight="Bold" Foreground="#334155"/>
                <TextBox x:Name="TxtWorkbookPath" Grid.Column="1" Height="36" Margin="8,0" VerticalContentAlignment="Center" Padding="10,0"/>
                <Button Grid.Column="2" Content="찾아보기" Width="100" Height="36" Margin="0,0,8,0" Click="BtnBrowse_Click"/>
                <Button x:Name="BtnRun" Grid.Column="3" Content="자동 생성 실행" Width="130" Height="36" Style="{StaticResource PrimaryButton}" Click="BtnRun_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8">
            <DataGrid x:Name="LogGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      AlternatingRowBackground="#F8FAFC" GridLinesVisibility="Horizontal" BorderThickness="0">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F1F5F9"/>
                        <Setter Property="Foreground" Value="#334155"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Height" Value="42"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="등록행" Binding="{Binding RowNo}" Width="100"/>
                    <DataGridTextColumn Header="등록번호" Binding="{Binding RegNo}" Width="150"/>
                    <DataGridTextColumn Header="결과" Binding="{Binding Result}" Width="150"/>
                    <DataGridTextColumn Header="메시지" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>