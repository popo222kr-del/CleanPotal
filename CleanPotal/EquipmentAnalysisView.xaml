<UserControl x:Class="CleanPotal.EquipmentAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             Background="#F8FAFC"
             SnapsToDevicePixels="True" UseLayoutRounding="True"
             TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
    <UserControl.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="Btn" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="Background" Value="#F1F5F9"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource Btn}">
            <Setter Property="Background" Value="#2563EB"/><Setter Property="Foreground" Value="White"/><Setter Property="BorderBrush" Value="#2563EB"/>
        </Style>
        <Style x:Key="Lbl" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/><Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="VerticalAlignment" Value="Center"/><Setter Property="Margin" Value="10,0,4,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- toolbar -->
            <RowDefinition Height="Auto"/>  <!-- filter -->
            <RowDefinition Height="*"/>     <!-- chart -->
            <RowDefinition Height="Auto"/>  <!-- table -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Padding="12,10">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBlock x:Name="TxtCount" Style="{StaticResource Lbl}" Margin="0,0,12,0"/>
                    <Button x:Name="BtnUpload" Content="엑셀 업로드" Style="{StaticResource PrimaryBtn}" Click="BtnUpload_Click"/>
                    <Button x:Name="BtnDownload" Content="엑셀 다운로드" Style="{StaticResource Btn}" Margin="6,0,0,0" Click="BtnDownload_Click"/>
                    <Button x:Name="BtnClear" Content="전체 삭제" Style="{StaticResource Btn}" Margin="6,0,0,0" Click="BtnClear_Click"/>
                </StackPanel>
                <TextBlock Text="설비 분석 DATA (ICP-MS)" FontSize="15" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- Filter bar -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="12,8">
            <WrapPanel Orientation="Horizontal">
                <TextBlock Text="보기" Style="{StaticResource Lbl}" Margin="0,0,4,0"/>
                <ComboBox x:Name="CmbMode" Width="130" SelectionChanged="Filter_Changed">
                    <ComboBoxItem Content="설비별 비교" IsSelected="True"/>
                    <ComboBoxItem Content="시간 추이"/>
                </ComboBox>
                <TextBlock Text="공정" Style="{StaticResource Lbl}"/>
                <ComboBox x:Name="CmbProcess" Width="90" SelectionChanged="Filter_Changed"/>
                <TextBlock Text="약액" Style="{StaticResource Lbl}"/>
                <ComboBox x:Name="CmbBath" Width="90" SelectionChanged="Filter_Changed"/>
                <TextBlock Text="원소" Style="{StaticResource Lbl}"/>
                <ComboBox x:Name="CmbElement" Width="80" SelectionChanged="Filter_Changed"/>
                <TextBlock x:Name="LblEq" Text="설비" Style="{StaticResource Lbl}"/>
                <ComboBox x:Name="CmbEquip" Width="110" SelectionChanged="Filter_Changed"/>
            </WrapPanel>
        </Border>

        <!-- Chart -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <Grid>
                <lvc:CartesianChart x:Name="Chart"/>
                <TextBlock x:Name="TxtEmpty" Text="데이터가 없습니다. '엑셀 업로드'로 분석 데이터를 추가하세요."
                           Foreground="#94A3B8" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="Collapsed"/>
            </Grid>
        </Border>

        <!-- Data table -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid" AutoGenerateColumns="True" IsReadOnly="True" MaxHeight="240"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" FontSize="12"
                      Background="White" BorderThickness="0" RowHeaderWidth="0"
                      CanUserAddRows="False" AlternatingRowBackground="#FAFAFA"/>
        </Border>
    </Grid>
</UserControl>
