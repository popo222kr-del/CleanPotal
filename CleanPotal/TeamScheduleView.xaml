<UserControl x:Class="CleanPotal.TeamScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CleanPotal"
             Background="#F1F5F9" SnapsToDevicePixels="True" UseLayoutRounding="True">

    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid Margin="24,12,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <UniformGrid Grid.Row="0" Columns="7" Margin="0,0,0,12">
                <TextBlock Text="일" Foreground="#EF4444" FontWeight="Bold" FontSize="15" TextAlignment="Center"/>
                <TextBlock Text="월" Foreground="#475569" FontWeight="Bold" FontSize="15" TextAlignment="Center"/>
                <TextBlock Text="화" Foreground="#475569" FontWeight="Bold" FontSize="15" TextAlignment="Center"/>
                <TextBlock Text="수" Foreground="#475569" FontWeight="Bold" FontSize="15" TextAlignment="Center"/>
                <TextBlock Text="목" Foreground="#475569" FontWeight="Bold" FontSize="15" TextAlignment="Center"/>
                <TextBlock Text="금" Foreground="#475569" FontWeight="Bold" FontSize="15" TextAlignment="Center"/>
                <TextBlock Text="토" Foreground="#3B82F6" FontWeight="Bold" FontSize="15" TextAlignment="Center"/>
            </UniformGrid>

            <Border Grid.Row="1" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="12" Background="#F8FAFC" Padding="8">
                <ItemsControl x:Name="CalendarItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="7" Rows="6" Background="Transparent"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Margin="4" Background="White">
                                <Grid ClipToBounds="True">

                                    <Border Background="#EFF6FF" BorderBrush="#3B82F6" BorderThickness="2" CornerRadius="7" Visibility="{Binding IsToday, Converter={StaticResource BoolToVisibilityConverter}}"/>

                                    <StackPanel Margin="6">
                                        <Grid Margin="4,4,0,8" HorizontalAlignment="Left">
                                            <Grid.Style>
                                                <Style TargetType="Grid">
                                                    <Setter Property="Opacity" Value="1"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCurrentMonth}" Value="False">
                                                            <Setter Property="Opacity" Value="0.55"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Grid>
                                                <Border Width="28" Height="28" CornerRadius="14" Background="#2563EB" Visibility="{Binding IsToday, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                <TextBlock Text="{Binding DayNumber}" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#334155"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Date.DayOfWeek}" Value="Sunday">
                                                                    <Setter Property="Foreground" Value="#DC2626"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Date.DayOfWeek}" Value="Saturday">
                                                                    <Setter Property="Foreground" Value="#2563EB"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding IsHoliday}" Value="True">
                                                                    <Setter Property="Foreground" Value="#DC2626"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding IsCurrentMonth}" Value="False">
                                                                    <Setter Property="Foreground" Value="#CBD5E1"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                    <Setter Property="FontWeight" Value="ExtraBold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>

                                            <Border Grid.Column="1" Background="#FEE2E2" CornerRadius="4" Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Center" Visibility="{Binding IsHoliday, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock Text="{Binding HolidayName}" FontSize="10" FontWeight="Bold" Foreground="#B91C1C"/>
                                            </Border>

                                            <!-- 팀 일정: 공휴일 없을 때 같은 위치에 표시 -->
                                            <Button Grid.Column="1" Click="Badge_Click" CommandParameter="{Binding TeamEventHeaderBadge}"
                                                    Background="Transparent" BorderThickness="0" Padding="0" Margin="8,0,0,0"
                                                    Visibility="{Binding HasTeamEventHeader, Converter={StaticResource BoolToVisibilityConverter}}"
                                                    ToolTip="{Binding TeamEventHeaderBadge.TooltipText}" Cursor="Hand" VerticalAlignment="Center">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="#DBEAFE" CornerRadius="4" Padding="6,2">
                                                            <TextBlock Text="{Binding TeamEventHeaderBadge.Text}" FontSize="10" FontWeight="Bold" Foreground="#1D4ED8" TextTrimming="CharacterEllipsis"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>

                                            <Border Grid.Column="2" Background="#DBEAFE" CornerRadius="4" Padding="6,2" Margin="6,0,0,0" VerticalAlignment="Center" Visibility="{Binding IsToday, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock Text="오늘" FontSize="11" FontWeight="ExtraBold" Foreground="#1E40AF"/>
                                            </Border>
                                        </Grid>

                                        <ItemsControl ItemsSource="{Binding Badges}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <UniformGrid Columns="2" VerticalAlignment="Top"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Click="Badge_Click" CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Padding="0" Margin="2" Cursor="Hand" HorizontalContentAlignment="Stretch" ToolTip="{Binding TooltipText}">
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{Binding BackgroundBrush}" CornerRadius="4" Padding="4,4">
                                                                    <TextBlock Text="{Binding Text}" Foreground="{Binding TextBrush}" FontWeight="{Binding FontWeight}" FontSize="11" TextAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </Grid>

        <Border x:Name="ModalOverlay" Background="#80111827" Visibility="Collapsed" Panel.ZIndex="100">
            <Border Background="#FFFFFF" BorderThickness="0" CornerRadius="16" Width="550" MaxHeight="650" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="20" ShadowDepth="5"/>
                </Border.Effect>
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,16">
                        <Grid>
                            <TextBlock x:Name="TxtModalTitle" Text="상세 명단" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                            <Button Content="✕" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" FontSize="18" FontWeight="Bold" Foreground="#64748B" Cursor="Hand" Click="BtnCloseModal_Click"/>
                        </Grid>
                        <TextBlock Text="항목별 우측의 삭제 버튼을 눌러 개별 일정을 취소할 수 있습니다." FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>

                    <DataGrid x:Name="DetailDataGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" RowBackground="White" AlternatingRowBackground="#F8FAFC" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E2E8F0" BorderThickness="1" BorderBrush="#CBD5E1" RowHeight="44">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F1F5F9"/>
                                <Setter Property="Foreground" Value="#334155"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Height" Value="40"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="성명" Binding="{Binding Name}" Width="1*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="일정 상세" Binding="{Binding Type}" Width="2*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Foreground" Value="#475569"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="관리" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="삭제" Background="#FEE2E2" Foreground="#DC2626" BorderBrush="#FCA5A5" BorderThickness="1" Width="60" Height="30" Cursor="Hand" FontWeight="Bold" Click="BtnDeleteDetail_Click">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="6"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>