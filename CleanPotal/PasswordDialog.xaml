<Window x:Class="CleanPotal.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="관리자 권한 확인" Width="340" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Background="#F5F7FB">

    <Grid Margin="20,20,20,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="관리자 권한 필요" FontSize="18" FontWeight="Bold" Foreground="#111827"/>
            <TextBlock Text="설정을 변경하려면 비밀번호를 입력하세요." FontSize="12" Foreground="#6B7280" Margin="0,4,0,0"/>
        </StackPanel>

        <PasswordBox x:Name="PwBox" Grid.Row="1" Height="38" Padding="10,8" FontSize="14" 
                     VerticalContentAlignment="Center"
                     BorderBrush="#D6DCE8" BorderThickness="1" Background="#FFFFFF" 
                     KeyDown="PwBox_KeyDown"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,20,0,0">
            <Button Content="취소" Width="70" Height="36" Background="#FFFFFF" BorderBrush="#D6DCE8" BorderThickness="1" Margin="0,0,8,0" Click="BtnCancel_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="확인" Width="70" Height="36" Background="#2563EB" Foreground="White" BorderThickness="0" Click="BtnConfirm_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>