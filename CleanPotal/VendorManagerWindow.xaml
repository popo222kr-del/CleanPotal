<Window x:Class="CleanPotal.VendorManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="업체 기준 정보 관리" Width="1100" Height="820"
        WindowStartupLocation="CenterOwner" Background="#F9FAFB"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        SnapsToDevicePixels="True" UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="ModernListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="16,12"/>
                        <Setter Property="Margin" Value="0,0,0,4"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Foreground" Value="#374151"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" SnapsToDevicePixels="True">
                                        <Grid>
                                            <ContentPresenter x:Name="contentPresenter" Margin="{TemplateBinding Padding}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                            <Rectangle x:Name="selectionIndicator" Fill="#2563EB" Width="4" HorizontalAlignment="Left" Visibility="Collapsed" RadiusX="2" RadiusY="2" Margin="0,8"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F3F4F6"/>
                                            <Setter Property="Foreground" Value="#111827"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                                            <Setter TargetName="selectionIndicator" Property="Visibility" Value="Visible"/>
                                            <Setter Property="Foreground" Value="#2563EB"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FAFB"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F3F4F6"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#475569"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#EFF6FF"/>
                                <Setter Property="Foreground" Value="#1E3A8A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="업체 기준 정보 관리" FontSize="26" FontWeight="ExtraBold" Foreground="#111827" Margin="0,0,0,24"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="등록된 업체" FontSize="16" FontWeight="Bold" Margin="0,0,0,12" Foreground="#1F2937"/>
                <Border Grid.Row="1" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="12" Background="White">
                    <ListBox x:Name="VendorListBox" Style="{StaticResource ModernListBoxStyle}" DisplayMemberPath="VendorName" SelectionChanged="VendorListBox_SelectionChanged"/>
                </Border>
                <Button Grid.Row="2" Content="+ 새 업체 추가" Style="{StaticResource MacSecondaryButtonStyle}" Margin="0,12,0,0" Click="AddVendor_Click"/>
            </Grid>

            <Border Grid.Column="2" Style="{StaticResource ModernCardStyle}">
                <Grid Cursor="">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="업체 상세 정보" FontSize="20" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,24"/>

                    <StackPanel Grid.Row="1" Margin="0,0,0,24">
                        <TextBlock Text="업체명" FontWeight="Bold" Margin="0,0,0,8" Foreground="#4B5563"/>
                        <TextBox x:Name="TxtVendorName" Height="38" Style="{StaticResource MacInputTextBoxStyle}"/>
                    </StackPanel>

                    <TextBlock Grid.Row="2" Text="사업장 및 공장 주소" FontWeight="Bold" Margin="0,0,0,8" Foreground="#4B5563"/>

                    <DataGrid x:Name="DetailGrid" Grid.Row="3" Margin="0,0,0,24" Style="{StaticResource ModernDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="기본" Binding="{Binding IsMain}" Width="50"/>
                            <DataGridTextColumn Header="구분" Binding="{Binding LocationName}" Width="150"/>
                            <DataGridTextColumn Header="주소" Binding="{Binding FullAddress}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Grid.Row="4" Text="담당자 목록" FontWeight="Bold" Margin="0,0,0,8" Foreground="#4B5563"/>

                    <DataGrid x:Name="ManagersDataGrid" Grid.Row="5" Margin="0,0,0,24" Style="{StaticResource ModernDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="담당자 이름" Binding="{Binding ManagerName}" Width="150"/>
                            <DataGridTextColumn Header="연락처" Binding="{Binding ContactNumber}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="6">
                        <Button Content="업체 삭제" HorizontalAlignment="Left" Style="{StaticResource MacDangerButtonStyle}" Width="100" Click="DeleteVendor_Click"/>
                        <Button Content="저장하기" HorizontalAlignment="Right" Style="{StaticResource MacPrimaryButtonStyle}" Width="120" Click="SaveVendor_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>